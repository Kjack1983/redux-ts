# React typescript application using registry.npmjs API.

## Branches
* `master`

## Setup

npm install. Use npm start to start the development server on port 3000.

## Tech stack: React hooks useDispatch, useSelector, Redux, react-redux, thunks. 